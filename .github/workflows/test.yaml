on:
  push:
    branches:
      - 'master'
jobs:
    test:
      name: Run
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v1
        - name: Run
          env:
            BEAMMP_SECRET_SENTRY_URL: ${{ secrets.BEAMMP_SECRET_SENTRY_URL }}
          run: |
            echo -e $BEAMMP_SECRET_SENTRY_URL
